<%= form_with url: month_chat_messages_path(month), method: :post, id: "message-form", class: "d-flex gap-2" do |f| %>
  <input type="text" name="message[content]" class="form-control" placeholder="Type your question..." required autofocus>
  <button type="submit" class="btn btn-primary">Send</button>
<% end %>
